info:
  name: checkStatusToken
  type: http
  seq: 3

http:
  method: get
  url: "{{baseUrl}}{{unprotected}}/fonnte/check-otp-status/REG-6271"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
